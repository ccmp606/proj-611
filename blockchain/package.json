{
  "name": "proj611-blockchain",
  "version": "1.0.0",
  "description": "Smart contract for PROJ-611 Blockchain-Based Cloud Logging System",
  "scripts": {
    "compile":  "hardhat compile",
    "test":     "hardhat test",
    "deploy":   "hardhat run scripts/deploy.js --network sepolia",
    "verify":   "hardhat run scripts/verify.js --network sepolia",
    "smoke":    "hardhat run scripts/test-contract.js --network sepolia",
    "node":     "hardhat node",
    "clean":    "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  }
}
